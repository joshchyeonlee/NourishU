import { Typography, Box, Button } from "@mui/material";

const CreateIngredient = () => {
    return(
        <Box display="flex" justifyContent="center" padding={4}>
            <Typography variant="h5">Create Ingredient</Typography>
        </Box>
    )
}

export default CreateIngredient;